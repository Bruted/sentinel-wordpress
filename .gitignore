# OS / editor
.DS_Store
Thumbs.db
*.log
.idea/
.vscode/
*.swp

# Dependencies / build output
node_modules/
vendor/
dist/
build/
*.egg-info/
__pycache__/
.env
.env.local

# Swift / Xcode
.build/
.swiftpm/
xcuserdata/
DerivedData/
*.xcuserstate

# Built plugin bundles. Attach these to a GitHub Release instead of
# tracking them: each rebuild is a new binary blob in history, and git
# stores them whole rather than as diffs.
*.zip
